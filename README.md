# homebrew-tap

Homebrew formulae for [vivace](https://github.com/svandragt/vivace), a fast `composer install` from `composer.lock`.

```sh
brew install svandragt/tap/vivace
```

The vivace release workflow updates `Formula/vivace.rb` on every release. Do not edit it by hand.
